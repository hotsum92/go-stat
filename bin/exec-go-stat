#!/bin/bash

path=$1

echo
git log --oneline --no-decorate HEAD^..HEAD
gocyclo --top 10 "$path" | sed 's/^/cycle /' || true
gocognit --top 10 "$path" | sed 's/^/cognit /' || true
go test --cover "$path" | sed 's/^/test /' || true
